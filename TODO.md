# **WAMP todo:**

1. in the output recorder module, the recording timeline does not visually populate when audio is recorded.
2. match second "drum pads" header(just above "through pedal chain") font, size, and color to "drum machine" header. Rename "Drum machine" to "SEQUENCER" in ui. rename main rack tool module header "drum pads" in ui to "DRUM PADS / SEQUENCER". Move THE DROP down pad selection menu(for the sequencer) to the left of each respective lane button. Increase the size of the individual pads in the 4x4 drum pads to 140% current size
3. Add a 1 row kit selection menu underneath the drum pads hot key instructions. include the ability to import/export current drum pad kit sounds and sequencer setup(separate from other tool and pedal saves). Also create: a "Trance Giant" kit, and 3 more preset drum pad kits from popular drum machine sounds for the user to select from(keeping the current default kit "Default").
4. change the looper trim interaction sliders so that they are a slidable, labeled vertical bar on the visual looper edit window. make similar changes for the section looper/practice trim interaction sliders. Add the ability to change the speed of the song up or down with a slider(.25x to 2.5x).
5. Combine Input monitor and output safety into one rack tool module, with input tools on left half of module and output tools on the right half. Rename main minimized module header "INPUT MONITOR / OUTPUT SAFETY"
6. rename "mute mic" buttons to "Mute Input" in UI. rename Main(minimized) header for "METRONOME" TO "METRONOME / TUNER". On the secondary header that currently says "METRONOME / TUNER" change to "METRONOME". on the tuner tool in this module, fix the "TUNER" title to resemble the secondary header("METRONOME")
7. in the pedalboard live mode controls, move the "PANIC MUTE" button to the right of the spillover menu with a triple gap in between. Make this button 2.6x bigger.
8. Combine Amp channel and CAB IR MANAGER tools into one rack tool module, with AMP CHANNEL tools on left half of module and CAB IR MANAGER tools on the right half. Rename main minimized module header "AMP CHANNEL / CAB IR MANAGER"
9. 

